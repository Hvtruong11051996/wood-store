import React from 'react';
import HeaderTop from './HeaderTop';

function Header(props) {
    return (
        <div>
            <HeaderTop></HeaderTop>
        </div>
    );
}

export default Header;